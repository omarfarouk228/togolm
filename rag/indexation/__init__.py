"""Indexation stage: cleaning, chunking, embedding, ingestion.

Submodules are imported explicitly (e.g. ``from rag.indexation.embedder import
get_embedder``) to avoid loading the embedding model on package import.
"""
